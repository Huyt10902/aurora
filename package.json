{
  "name": "aurora",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack music app with PostgreSQL, JWT auth, S3-compatible media storage, playlists, chat, and recommendations.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:create": "node backend/src/db/create_db.js",
    "db:schema": "npm run db:schema --prefix backend",
    "seed:songs": "npm run seed:songs --prefix backend",
    "seed:albums": "npm run seed:albums --prefix backend",
    "storage:up": "docker compose up -d minio",
    "storage:down": "docker compose down",
    "lint:frontend": "npm run lint --prefix frontend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
